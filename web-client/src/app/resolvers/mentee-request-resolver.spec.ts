import {MenteeRequestResolver} from './mentee-request-resolver';

describe('MenteeRequestResolver', () => {
  it('should create an instance', () => {
    expect(new MenteeRequestResolver()).toBeTruthy();
  });
});
