import {ProfileRouteResolver} from './profile-route-resolver';

describe('ProfileRouteResolver', () => {
  it('should create an instance', () => {
    expect(new ProfileRouteResolver()).toBeTruthy();
  });
});
