import {TrainingPlansResolver} from './training-plans-resolver';

describe('TrainingPlansResolver', () => {
  it('should create an instance', () => {
    expect(new TrainingPlansResolver()).toBeTruthy();
  });
});
